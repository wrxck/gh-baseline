/**
 * The profile builder lives at `src/tui/views/ProfileBuilder.tsx` now.
 * This module is kept only as a redirect for any external imports during the
 * migration window. New consumers should import from `../views/ProfileBuilder.js`.
 */
export { ProfileBuilder } from '../views/ProfileBuilder.js';
